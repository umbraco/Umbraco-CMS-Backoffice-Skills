---
description: Create a new Umbraco instance using the PSW CLI
argument-hint: "[project-name]"
allowed-tools: Bash
---

Create a new Umbraco instance called **$ARGUMENTS** using the package-script-writer skill.

Use the `package-script-writer` skill to generate and run the installation script.

If no project name is provided, use "UmbracoWeb" as the default.
